/**
 * @file CoreEvents.h
 * @brief Convenience header that pulls in the complete event system for core event definitions.
 *
 * Include this file to get access to EventSystem, EventTypes, and all supporting
 * macros (DEFINE_EVENT, CALL_EVENT, REGISTER_LISTENER, etc.) needed to declare
 * and work with core engine events.
 */

#include "ship/events/EventSystem.h"